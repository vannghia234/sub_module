dart run build_runner build --delete-conflicting-outputs -> lệnh run tạo ảnh, icon, anim...

dart run easy_localization:generate -f keys -o locale_keys.g.dart -> tạo file đa ngôn ngữ

flutter build apk --release --flavor production

https://avatar.iran.liara.run/public -> avatar dynamic

git clone --recurse-submodule URL- repo chinh, nó down code từ md chính tới submd
cách 2:
git submodule init
git submodule update

Cập Nhật Submodule trong Dự Án Chính
Nếu submodule có thay đổi (từ bạn hoặc cộng tác viên), bạn cần cập nhật submodule trong dự án chính:

git submodule update --init --remote

https://via.placeholder.com/150

-> update 26/12/2024
